import type { FC } from 'react';
import PageNav from '../components/PageNav';

const Pricing: FC = () => {
 return (<div>
    <PageNav/>
    <h1>
        Pricing
    </h1>
  
  </div>);
};

export default Pricing;
